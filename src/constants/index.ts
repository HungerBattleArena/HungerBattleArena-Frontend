import { QueryClient } from '@tanstack/react-query';

export const queryClient = new QueryClient();
export const APP_URL = 'https://hungerfe.hedos.finance/'
export const ONE_WALLET_URL = 'https://one-wallet.cc/en/'